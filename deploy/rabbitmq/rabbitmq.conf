# ==============================================================================
# RabbitMQ configuration
# ==============================================================================
# Per-service users are created by init.sh on every start using rabbitmqctl.
# The old definitions.json approach was removed (CRITICAL-1) because it
# hardcoded user credentials that ignored .env overrides.
# ==============================================================================

vm_memory_high_watermark.relative = 0.5
